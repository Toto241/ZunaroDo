# Play Console Screenshots

Nach dem Closed Test hier ablegen:

- `closed-test-YYYY-MM-DD.png` — Screenshot aus der Play Console (Tester-Anzahl, Zeitraum, Crash-free rate)

Referenziert von `release/closed-test-*.md` für das Release-Gate.
