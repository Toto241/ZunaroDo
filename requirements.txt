# Pflicht-Abhaengigkeiten
customtkinter>=5.2.0
fpdf2>=2.7.0

# Proaktive Benachrichtigungen
APScheduler>=3.10.0

# Sichere Geraetekopplung (services/pairing/) - Ed25519 + Secure-Store + SPAKE2
cryptography>=42.0.0
keyring>=24.0.0
spake2>=0.8

# Optional: Desktop-Notifikationen (faellt sanft auf print zurueck)
plyer>=2.1.0

# Optional: Google Gemini als KI-Backend
# google-generativeai>=0.7.0

# Optional: OCR fuer Kassenbons
# pytesseract>=0.3.10
# Pillow>=10.0.0

# Optional: verschluesselte DB (ALLTAGSHELFER_DB_KEY setzen, um sie zu nutzen)
# sqlcipher3-binary>=0.5.0
